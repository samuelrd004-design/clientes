# Clientes

Páginas de clientes publicadas automaticamente via Netlify.

Para adicionar um cliente novo, crie uma pasta `nome-do-cliente/` com um `index.html` dentro, depois `git add`, `git commit` e `git push`.
